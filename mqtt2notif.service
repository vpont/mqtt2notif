[Unit]
Description=mqtt2notif - MQTT to Linux Notification Receiver
After=network.target

[Service]
ExecStart=%h/.local/bin/mqtt2notif.py --daemon
Restart=always
RestartSec=10
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=default.target
